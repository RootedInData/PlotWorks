# Tests

From the directory containing `Data_analysis_agency/`, run:

```bash
python -m unittest discover -s Data_analysis_agency/tests -v
```

The included tests exercise polished Python plotting, managed output filenames,
and custom R code validation. They do not require R to be installed.
